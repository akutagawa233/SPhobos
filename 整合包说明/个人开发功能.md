# 1.建造前提描述
在 `rulesmd.ini` 之中：
```ini
[SOMETECHNO] ;任意单位
UIPrerequisite= ;单位列表,工具描述上会显示列表上的所有单位
```
在 `uimd.ini` 之中：
```ini
[ToolTips]
PrerequisiteLabel= ;CSF条目,建造前提的标题
PrerequisiteYes=TXT:PrerequisiteYes ;CSF条目,当某条建造前提达到时显示的文字
PrerequisiteNo=TXT:PrerequisiteNo ;CSF条目,当某条建造前提没有达到时显示的文字
```
